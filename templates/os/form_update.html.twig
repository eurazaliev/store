{# src/templates/os/form_update.html.twig #}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block title %}os{% endblock %}
{% block body %}
<div class="blog-header">
    <h2 class="blog-title text-center">Edit {{name}}</h2>
</div>

<div>
{% for label, messages in app.flashes %}
    {% for message in messages %}
{#	<div class="alert alert-warning" role="alert"> #}
        <div class="alert alert-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
</div>


<div class="container h-50">
<div class="row h-50 justify-content-center align-items-center">
<div class="col-sm-12 blog-main justify-content-center align-items-center">
{{ form_start(form, { 'action': path('os_edit',{'id': id}), 'method': 'POST', 'attr': {'class': 'os'} }) }}

{#    {{ form_widget(form) }} #}
<div class="col-md-12">
    <div class="form-group row">
    <div class="form-group">
            {{ form_row(form.name) }}
    </div>
    </div>

    <div class="form-group row">
    <div class="form-group">
            {{ form_row(form.version) }}
    </div>
    </div>

    <div class="form-group row">
    <div class="form-group">
            {{ form_row(form.platform_id) }}
    </div>
    </div>

    <div class="form-group row">
    <div class="form-group">
            {{ form_row(form.vendor_id) }}
    </div>
    </div>

    <div class="form-group row">
    <div class="form-group">
            {{ form_row(form.language_id) }}
    </div>
    </div>


    <div class="form-group row">
    <div class="form-group">
            {{ form_row(form.memo) }}
    </div>
    </div>

    <div class="form-row justify-content-center">
    <div class="form-group text-center">
        <input type="submit" value="Edit">
    </div>
    </div>
</div>{{ form_end(form) }} 
</div>
</div>
</div>
{% endblock %}
